[project]
name = "impervious"
version = "0.2.0"
description = "Imperviousness segmentation pipeline (Sentinel-2 + OSM, YOLO-seg)"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["impervious"]
